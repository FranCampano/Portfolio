import { Routes } from '@angular/router';
import { LandingPage } from './landing-page/landing-page';

export const routes: Routes = [
  { path: '', component: LandingPage },  // Ruta principal
  { path: '', component: LandingPage },  // Ruta principal
  { path: '', component: LandingPage },  // Ruta principal

];
